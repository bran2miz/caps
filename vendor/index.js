'use strict';

//create the variable that is an instance of the vendor's handler or pickupEvent
const vendorHandler = require('./handler');

//export it to be used in the logger. 
module.exports = vendorHandler;